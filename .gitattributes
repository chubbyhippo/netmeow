# Normalize text files to LF in the repository (WSL + Windows tools otherwise
# leave the working tree littered with CRLF-only diffs, and Spotless fails on
# every file at once).
* text=auto

# Scripts are run by sh directly — LF on every checkout.
*.sh text eol=lf

# The Windows batch launcher (if any) needs CRLF.
*.bat text eol=crlf
